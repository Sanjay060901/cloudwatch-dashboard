services:
  backend:
    build: ./backend
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    env_file:
      - .env
    ports:
      - "8080:80"
    restart: unless-stopped
